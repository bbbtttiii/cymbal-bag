<%= form_for @bag, url: new_bag_path() do |f| %>

    <%= f.label "Name (ex: 'Main Kit', 'Jazz', etc):" %>
    <br>
    <%= f.text_field :name %>
    <br><br>
    <%= f.submit "Create bag & add cymbals ->" %>

<% end %>
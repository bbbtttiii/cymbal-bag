<%= form_for @bag do |f| %>

    <%= f.hidden_field :user_id %>

    <%= f.label "Name (ex: 'Main Kit', 'Jazz', etc):" %>
    <br>
    <%= f.text_field :name %>
    <br><br>
    <%= f.submit "Create bag & add cymbals ->" %>

<% end %>
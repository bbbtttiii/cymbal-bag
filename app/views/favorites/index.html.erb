<h1>Cymbals </h1>

<% if @favorites.empty? %>
    None created yet!
<% else %>

    <ul>
    <% @favorites.each do |b| %>

        <li><%= link_to b.name, favorite_path(b) %></li>

    <% end %>
    </ul>

<% end %>